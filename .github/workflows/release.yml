name: Automated Multi-Platform Release Build

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-release:
    name: Build & Release (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: android
            os: ubuntu-latest
            build_cmd: flutter build apk --release
            artifact_path: build/app/outputs/flutter-apk/app-release.apk
            asset_name: ShopManagement-Android.apk

          - target: windows
            os: windows-latest
            build_cmd: flutter build windows --release
            artifact_path: build/windows/x64/runner/Release/
            asset_name: PerfectSolution-Setup.exe

          - target: macos
            os: macos-latest
            build_cmd: flutter build macos --release
            artifact_path: build/macos/Build/Products/Release/
            asset_name: ShopManagement-macOS.dmg

          - target: web
            os: ubuntu-latest
            build_cmd: flutter build web --release
            artifact_path: build/web
            asset_name: web-build

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Java SDK
        if: matrix.target == 'android'
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Enable Desktop Support
        run: flutter config --enable-macos-desktop --enable-windows-desktop

      - name: Install Dependencies
        run: flutter pub get

      - name: Prepare macOS Native Configs
        if: matrix.target == 'macos'
        run: |
          cd macos
          pod install --repo-update
          cd ..

      - name: Build Target Artifact
        run: ${{ matrix.build_cmd }}

      - name: Copy VCRUNTIME DLLs into Release folder
        if: matrix.target == 'windows'
        shell: powershell
        run: |
          # Find VC++ runtime DLLs from Visual Studio installation and copy them
          # alongside the app so they are bundled in the installer
          $releaseDir = "build\windows\x64\runner\Release"
          $vsDirs = @(
            "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Redist\MSVC",
            "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Redist\MSVC",
            "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Redist\MSVC"
          )
          $dlls = @("vcruntime140.dll","vcruntime140_1.dll","msvcp140.dll","msvcp140_1.dll","msvcp140_2.dll")
          foreach ($vsDir in $vsDirs) {
            if (Test-Path $vsDir) {
              $x64Dirs = Get-ChildItem -Path $vsDir -Recurse -Filter "x64" -Directory | Where-Object { $_.FullName -like "*CRT*" }
              foreach ($dir in $x64Dirs) {
                foreach ($dll in $dlls) {
                  $src = Join-Path $dir.FullName $dll
                  $dst = Join-Path $releaseDir $dll
                  if ((Test-Path $src) -and -not (Test-Path $dst)) {
                    Copy-Item $src $dst -Force
                    Write-Host "Copied $dll from $($dir.FullName)"
                  }
                }
              }
              break
            }
          }
          # Verify critical DLLs exist
          foreach ($dll in @("vcruntime140.dll","vcruntime140_1.dll","msvcp140.dll")) {
            if (Test-Path (Join-Path $releaseDir $dll)) {
              Write-Host "OK: $dll present in release folder"
            } else {
              Write-Warning "MISSING: $dll not found - app may fail on machines without VC++ runtime"
            }
          }

      - name: Package Windows Installer
        if: matrix.target == 'windows'
        shell: powershell
        run: |
          choco install innosetup --yes --no-progress
          $issContent = @"
[Setup]
AppName=Perfect Solution
AppVersion=1.0.7
AppPublisher=Perfect Solution
DefaultDirName={autopf}\Perfect Solution
DefaultGroupName=Perfect Solution
UninstallDisplayIcon={app}\shop_management_flutter.exe
Compression=lzma2
SolidCompression=yes
OutputDir=.
OutputBaseFilename=PerfectSolution-Setup
ArchitecturesInstallIn64BitMode=x64
MinVersion=10.0

[Files]
Source: "build\windows\x64\runner\Release\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs

[Icons]
Name: "{autoprograms}\Perfect Solution"; Filename: "{app}\shop_management_flutter.exe"
Name: "{autodesktop}\Perfect Solution"; Filename: "{app}\shop_management_flutter.exe"; Tasks: desktopicon

[Tasks]
Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"

[Run]
Filename: "{app}\shop_management_flutter.exe"; Description: "{cm:LaunchProgram,Perfect Solution}"; Flags: nowait postinstall skipifsilent
"@
          Set-Content -Path installer.iss -Value $issContent -Encoding UTF8
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss

      - name: Package macOS DMG
        if: matrix.target == 'macos'
        run: |
          mkdir -p dist_macos
          cp -R "build/macos/Build/Products/Release/Perfect Solution.app" dist_macos/ || cp -R build/macos/Build/Products/Release/*.app dist_macos/
          brew install create-dmg || true
          create-dmg \
            --volname "Perfect Solution" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "Perfect Solution.app" 175 120 \
            --hide-extension "Perfect Solution.app" \
            --app-drop-link 425 120 \
            "PerfectSolution-macOS.dmg" \
            "dist_macos/" || hdiutil create -volname "Perfect Solution" -srcfolder "dist_macos/" -ov -format UDZO PerfectSolution-macOS.dmg

      - name: Upload Artifacts to GitHub Release
        if: matrix.target != 'web'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ matrix.target == 'android' && 'build/app/outputs/flutter-apk/app-release.apk' || '' }}
            ${{ matrix.target == 'windows' && 'PerfectSolution-Setup.exe' || '' }}
            ${{ matrix.target == 'macos' && 'PerfectSolution-macOS.dmg' || '' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy Web to GitHub Pages
        if: matrix.target == 'web'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
